# Runtime requirements for the offline pipeline (main.py / utils.py) and the
# ART/ training scripts. Tested on macOS with Python 3.10 / 3.12 and CPU-only
# (Apple Silicon MPS is auto-detected when present).
#
# Mac install (CPU / MPS):
#     pip install -r requirements.txt
#
# If you specifically need the CPU wheels of torch on Linux/Windows:
#     pip install torch --index-url https://download.pytorch.org/whl/cpu
#     pip install -r requirements.txt
#
# Pins are kept loose so a wider range of Python versions works; the lower
# bounds are the lowest versions we have verified against.

torch>=2.0
numpy>=1.24
scipy>=1.10
matplotlib>=3.7
seaborn>=0.12
